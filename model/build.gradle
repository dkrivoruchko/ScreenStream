apply plugin: 'java'
apply plugin: 'me.tatarka.retrolambda'

sourceCompatibility = "1.8"
targetCompatibility = "1.8"

dependencies {
    // Rx
    compile dep.rxjava

    // Dagger
    compile dep.dagger
}