# Screen Capture Engine — Private Constants

## 1. Scope and authority

This document binds the seven private numeric constants required by
[05 Gate-B Inputs](05_gate_b_inputs.md#2-private-constants). It is subordinate to the product behavior in
[01 Design](01_design.md), the technical rules in [02 Architecture](02_architecture.md), and the verification
rules in [03 Verification](03_verification.md). A constant selects only when an already-defined boundary is
observed; it does not change the boundary's failure classification, ownership transition, lifecycle priority,
late-cleanup behavior, supported API range, or runtime-path selection.

All deadline values use the injected `EngineClock` time base corresponding to
`SystemClock.elapsedRealtimeNanos()`. They are positive, finite, private implementation policy. They are not a
public timing guarantee or service-level objective. The public callback-entry boundary and Stats publication
cadence remain unchanged and are not restated as private constants here.

This document defines no image value or tolerance and grants no implementation authorization.

## 2. Bound values

| Constant | Kotlin type and unit | Exact value | Precise use sites | Checked guard | Existing outcome and late-cleanup semantics | Symbolic boundary tests |
| --- | --- | ---: | --- | --- | --- | --- |
| `firstMetricsReadinessNanos` | `Long`; elapsed-realtime nanoseconds | `5_000_000_000L` | The sole startup readiness occurrence from accepted attachment of the one `CaptureMetricsProvider.observe()` collection until the controller owns the first valid combined width, height, and density tuple. The controller fixes this occurrence's `S` and `D` atomically with the accepted-attachment ownership commit, before collection can publish a fact. Later collection is long-lived and uses its mechanical collector-return rule, not this deadline. | Let `C` be this value and `S` the accepted-attachment commit sample. Require `C > 0`, `S >= 0`, and `S <= Long.MAX_VALUE - C`; then set `D = Math.addExact(S, C)` in that same gated commit. Guard failure is the existing internal checked-arithmetic `InternalFailure`, not a fabricated readiness expiry. | A valid tuple committed at `T < D` retires the occurrence and permits startup to continue. An empty occurrence observed at `now >= D`, or a tuple committed at `T >= D`, selects the existing startup `CaptureUnavailable` outcome and matching `start` failure. A later tuple cannot revive the Session and is cleanup-only. Provider failure, completion, cancellation intent, and collector mechanical return retain their existing classifications and ownership rules. | Validate the literal and guard; accept `S = Long.MAX_VALUE - C` with `D = Long.MAX_VALUE`; reject `S = Long.MAX_VALUE - C + 1` without wrap. For a safely representable `D`, cover a valid tuple at `D - 1`, `D`, and `D + 1`; an empty occurrence at `now = D`; expiry before a late valid tuple; an early/stale wake; and a valid tuple committed before controller signalling. |
| `initialCapturedResizeReadinessNanos` | `Long`; elapsed-realtime nanoseconds | `5_000_000_000L` | The sole API 34–37 startup readiness occurrence after the one `createVirtualDisplay` call has returned a nonnull display and while no timely valid initial `MediaProjection.Callback.onCapturedContentResize` fact is owned. The Android worker fixes `S` and `D` in precreated return-cell fields atomically with complete publication of that normal nonnull VirtualDisplay return and its owner reference; later controller application never resamples the start. A valid callback fact published earlier remains in the same identity-fenced readiness cell with its stored `T` and is accepted only when `T < D`. Runtime resize facts do not create this startup deadline. | Let `C` be this value and `S` the normal nonnull VirtualDisplay-return publication sample. Require `C > 0`, `S >= 0`, and `S <= Long.MAX_VALUE - C`; then set `D = Math.addExact(S, C)` and publish `S`, `D`, and the display owner together in that same complete return-cell commit. Guard failure is the existing internal checked-arithmetic `InternalFailure`, not resize expiry. | A valid initial resize committed at `T < D` retires the occurrence and permits reconciliation of authoritative capture dimensions. An empty occurrence at `now >= D`, or a resize committed at `T >= D`, selects the existing `Failed(CaptureUnavailable)` startup outcome, matching `start` exception, and required capability diagnostic attempt. No provisional frame is admitted, and a later resize cannot revive the Session. APIs 24–33 never arm this occurrence. | Validate the literal and guard; cover the exact maximum representable `D` and rejected addition overflow. On API 34–37, cover a valid resize at `D - 1`, `D`, and `D + 1`; empty-at-`D` expiry; an earlier callback fact whose stored `T` is compared with the return-cell's atomically published `D`; controller/drainer delay after return publication; expiry before late resize; an early/stale wake; and terminal selection racing the fact. Verify that APIs 24–33 create no occurrence. |
| `androidEnteredOperationSafetyNanos` | `Long`; elapsed-realtime nanoseconds | `5_000_000_000L` | Each finite preterminal Android platform-mutation occurrence on the Session-private Android lane: projection callback registration, the sole `createVirtualDisplay` call, `VirtualDisplay.resize`, `VirtualDisplay.setSurface`, and each `SurfaceTexture` listener installation or removal logical call. It also governs the typed preterminal `Surface.release()` occurrence after its closed prerequisites hold, although that call executes on the Session-private GL lane. One deadline covers one logical occurrence; no underlying call receives a second deadline. | At successful entered-latch commit, let `C` be this value and `S` the entry clock sample. Require `C > 0`, `S >= 0`, and `S <= Long.MAX_VALUE - C`; then set the occurrence's `D = Math.addExact(S, C)`. Addition failure uses existing internal checked-arithmetic `InternalFailure` and never wraps. | A complete normal or thrown return with settlement sample `T < D` uses its existing typed classification and receipts. A complete return with `T >= D`, or an empty return cell observed at `now >= D`, commits the existing timeout `InternalFailure` candidate subject to terminal priority. Real late facts reduce only exact cleanup or quarantine residue. In particular, a late normal `Surface.release()` return remains its physical receipt for dependent cleanup but cannot authorize production, fallback, replacement, or revival; a throw supplies no Surface receipt. | For each governed occurrence, cover entered arming and checked-addition limits; complete normal and thrown returns at `D - 1`, `D`, and `D + 1`; empty-at-`D` expiry; system return followed by worker pause before `settlementGate`; worker sample at `D - 1` followed by pause while holding the gate; complete commit before controller signalling; expiry before late normal/throw; stale-key timely normal return; scheduler rejection before entry; and terminal-before-entry versus terminal-after-entry retirement. |
| `glEnteredOperationSafetyNanos` | `Long`; elapsed-realtime nanoseconds | `10_000_000_000L` | One deadline per logical finite preterminal GL occurrence on the Session-private GL lane: EGL/GL session construction and validation; target or output render-resource construction and validation; one materialized frame's GL phase from current-context validation and `SurfaceTexture` acquisition through transform, canonical state, draw, Direct `glReadPixels`, and final error drain; and preterminal destruction of an incompatible GL scope after its leases close. It is not a per-GLES-call watchdog. `Surface.release()` uses the Android constant above. | At successful entered-latch commit, let `C` be this value and `S` the entry sample. Require `C > 0`, `S >= 0`, and `S <= Long.MAX_VALUE - C`; then set `D = Math.addExact(S, C)`. Addition failure uses the existing internal checked-arithmetic `InternalFailure` path and never creates a wrapped deadline. | A complete GL result at `T < D` retains the existing framebuffer, GL-error, allocation, currentness, and stale-work classifications. A complete result at `T >= D`, or an empty cell at `now >= D`, selects the existing unsafe timeout/ownership `InternalFailure` disposition; returned facts and destruction receipts are cleanup-only. No timeout creates a Downscaled or other fallback. Late facts cannot publish pixels, authorize reuse, or change lifecycle. | For every logical GL occurrence, cover checked-addition limits; success, returned GL failure, and typed receipt at `D - 1`, `D`, and `D + 1`; empty-at-`D`; pause before gate entry; pause after a `D - 1` sample while holding the gate; commit before signalling; expiry before late result; current versus stale operation identity; and terminal retirement with late cleanup-only evidence. A frame test also proves that one logical phase owns one deadline rather than one per GLES call. |
| `jpegEnteredOperationSafetyNanos` | `Long`; elapsed-realtime nanoseconds | `15_000_000_000L` | One deadline per finite preterminal JPEG occurrence. The single Native preparation occurrence enters immediately before `System.loadLibrary` for the engine's own bridge and remains the same occurrence through loader serialization, `JNI_OnLoad`, bootstrap/registration, the NDK-28 same-function `__builtin_available(android 30, *)` guard, the guarded weak `&AndroidBitmap_compress` address/null eligibility decision, carrier/backend selection, and complete preparation-result publication. The single Framework occurrence enters before either carrier-to-Bitmap fast transfer or portable row transfer and remains the same occurrence through `Bitmap.compress`, every transactional-sink write, and commit or abort settlement. The single Native encode occurrence enters before JNI entry and remains the same occurrence through the compressor call and native writer callbacks, post-compressor managed segment adoption and proven native-segment release, capture of every JNI/throwable/result-block fact, and complete JNI return-evidence publication. The exactly-once `Bitmap.recycle()` required before incompatible preterminal replacement is a separate occurrence using the same constant. The exactly-once `NativeMallocCarrier` free required before incompatible preterminal carrier replacement is another occurrence using the same constant. Terminal-origin or terminal-converted Bitmap retirement and carrier free use their same occurrences without a watchdog. | At successful entered-latch commit, let `C` be this value and `S` the entry sample. Require `C > 0`, `S >= 0`, and `S <= Long.MAX_VALUE - C`; then set `D = Math.addExact(S, C)`. Addition failure uses existing internal checked-arithmetic `InternalFailure` and never wraps. | A timely preparation result uses the existing clean own-bridge unavailability, OOM, poison, guarded weak-address eligibility, and carrier/backend-selection partitions. Preparation expiry or a late preparation result is the existing `InternalFailure` before any fresh attempt: it creates no frame and increments no frame-drop counter. Late preparation cannot publish a carrier/backend choice; its actual return evidence may reduce only exact cleanup or quarantine residue. Terminal conversion after preparation entry retires the deadline and retains the same writable occurrence under no-watchdog cleanup. A timely Framework or Native encode result uses its existing result partition, transaction rule, currentness fence, and Native-health rule. Encode expiry or late result retains the existing timeout/nonreturn `InternalFailure`, frame-drop, owner-retention, and quarantine behavior; late encode evidence publishes no bytes, changes no Native health, and never retries the frame. A timely normal recycle or carrier-free return is the physical receipt required before the corresponding preterminal replacement. Rejection, throw, expiry, or nonreturn retains the exact owner and occurrence and authorizes no replacement; a late normal return advances only exact cleanup. Terminal-origin or terminal-converted recycle and carrier-free cleanup remain no-watchdog operations. | For the complete Native preparation, Framework encode, Native encode, preterminal recycle, and preterminal incompatible-carrier-free occurrences, cover checked-addition limits and relevant results at `D - 1`, `D`, and `D + 1`, empty-at-`D`, worker pauses before gate entry and after a `D - 1` sample, commit before signalling, expiry before late return, and terminal conversion to no-watchdog cleanup. Preparation cases span own-bridge load, bootstrap/registration, the same-function availability guard, guarded weak-address/null eligibility, carrier/backend selection, and final publication; timeout proves no fresh attempt, frame, drop, carrier/backend publication, or active use of late evidence. Encode cases pause during Framework transfer/compress/transaction settlement and Native JNI/writer/adoption/segment-release/final publication, and cover stale-key timely Native disable versus late/retired-health no change and no same-frame retry. Recycle and carrier-free cases prove exactly-once entry; timely normal receipt versus rejection, throw, nonreturn, and late cleanup; and no deadline for terminal-origin or terminal-converted cleanup. A winning preterminal carrier-free timeout uses the existing `NativeJpeg` capability diagnostic and matching terminal-cause identity without introducing another category or numeric value. |
| `maxOldGlErrorsPerFrame` | `Int`; count of non-`GL_NO_ERROR` results | `8` | The bounded old-error drain after current-context validation and before current-frame acquisition or drawing. It counts only non-`GL_NO_ERROR` results. After at most this many recorded errors, one final sentinel probe determines whether the error flags are clean; there is no unbounded loop. | Require `1 <= C` and `C < Int.MAX_VALUE`; compute the maximum probe count as `Math.addExact(C, 1)`. Increment the `Int` error count only after a nonzero result and never beyond `C`. | Every recorded error remains input to the existing GL error classification; reaching `GL_NO_ERROR` does not erase recorded evidence. `GL_NO_ERROR` on the sentinel after exactly `C` errors proves the drain complete. A further nonzero sentinel result proves that the old-error set could not be boundedly cleared and uses the existing unsafe GL `InternalFailure` disposition. The cap does not authorize drawing, fallback, or reuse and does not reclassify any specific GL error. | Cover immediate `GL_NO_ERROR`; `C - 1` errors then `GL_NO_ERROR`; exactly `C` errors then sentinel `GL_NO_ERROR`; and exactly `C` errors then a nonzero sentinel. Assert at most `C + 1` `glGetError()` calls, no counter overflow, no unbounded polling, preservation of every recorded code through classification, and no current-frame GL work after cap exhaustion. |
| `maxFinalGlErrorsPerFrame` | `Int`; count of non-`GL_NO_ERROR` results | `8` | The bounded final error drain after the current frame's draw and Direct readback and before its GL result is published. It counts only non-`GL_NO_ERROR` results and performs at most one sentinel probe after reaching the cap. | Require `1 <= C` and `C < Int.MAX_VALUE`; compute the maximum probe count as `Math.addExact(C, 1)`. Increment the `Int` error count only after a nonzero result and never beyond `C`. | Every recorded error remains input to the existing final GL result classification. `GL_NO_ERROR` after exactly `C` errors completes observation without suppressing those errors. A further nonzero sentinel proves that final error state could not be boundedly cleared and uses the existing unsafe GL `InternalFailure` disposition. Neither reaching nor exhausting the cap can publish pixels, create a fallback, or convert failed evidence into success. | Cover immediate `GL_NO_ERROR`; `C - 1` errors then `GL_NO_ERROR`; exactly `C` errors then sentinel `GL_NO_ERROR`; and exactly `C` errors then a nonzero sentinel. Assert at most `C + 1` calls, no overflow or unbounded polling, preservation of all recorded final codes, no successful frame publication when the existing classifier rejects the evidence, and cleanup-only handling after terminal transfer. |

## 3. Interpretation and normative links

The five `Long` rows use the injected `EngineClock` and their row-specific checked `S + C` construction; no
deadline may wrap. A complete fact or return is timely only at `T < D`. Equality and every later sample belong to
expiry, while an empty occurrence expires when observed at `now >= D`. The two `Int` rows bound only the exact
error-drain probes and classifications stated in their rows.

The authoritative operation/readiness identities, gates, publication points, wake links, watchdog exclusions,
terminal transfer, and cleanup-only late-result mechanics are in
[06 Implementation Design §5](06_implementation_design.md#5-generic-operation-settlement-and-deadlines), with
the concrete owners in [§7](06_implementation_design.md#7-android-capture-metrics-and-target),
[§8](06_implementation_design.md#8-egl-gles2-direct-readback-and-color), and
[§10](06_implementation_design.md#10-jpeg-runtime-framework-adapter-native-bridge-and-storage). Timeout
visibility and diagnostic noncontrol behavior are owned by
[§12.3](06_implementation_design.md#123-diagnostics).

The executable boundary, race, cap, and diagnostic obligations are mapped in
[08 Test Implementation Map §4.1](08_test_implementation_map.md#41-deadline-wake-and-controller-matrices) and
[§6](08_test_implementation_map.md#6-deadline-diagnostics-and-noncontrol-behavior). Those documents reference
the exact values and row obligations here rather than redefining them.

These constants authorize no implementation work. Gate B and the separate implementation-authorization
checkpoint remain controlling.
